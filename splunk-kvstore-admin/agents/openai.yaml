interface:
  display_name: "Splunk KV Store Administration"
  short_description: "Back up, restore, migrate, and check the Splunk KV Store"
  default_prompt: >-
    Use $splunk-kvstore-admin to render and validate KV Store backup, restore,
    storage-engine migration, SHC member resync, and kvstore-status health
    assets for standalone search heads or search head clusters.
